//a
//  filter, find, search 


//d->
for (let i = 0; i < 3; i++) {
setTimeout(() => console.log("reminder for slot", i), 100);
}

// i =3 was loges, i used let insted of var so i can chnage and update its value in the for loop.



//c->
createSlotBook(capacity)
    booked= [1,3,7,2,9];
    book(patient);
    cancel(patientName)
    getBooked()=> return booked;
};
// as booked array is declared im the createSlotBook function and can only get access throuh the getBooked() that mnakes it private 